name: "Download All Weekly Target Comparison Artifacts"
description: "Downloads all the artifacts for all targets needed for run comparison"
inputs:
  gcchash:
    required: true
  token:
    required: true
  weekly_run:
    required: true

runs:
  using: "composite"
  steps:
    # Download linux

    - name: Download linux rv32 zve32f
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'zve' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zve32f-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zve32f-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv64 zve32f
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'zve' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zve32f-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zve32f-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv32 zve64d
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'zve' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zve64d-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zve64d-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv64 zve32f
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'zve' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zve64d-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zve64d-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}


    - name: Download linux rv64-128b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv64-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zvl128b-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zvl128b-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv64-256b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv64-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zvl256b-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zvl256b-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv64-512b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv64-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zvl512b-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zvl512b-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv64-1024b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv64-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv64gcv_zvl1024b-lp64d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv64gcv_zvl1024b-lp64d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}


    - name: Download linux rv32-128b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv32-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zvl128b-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zvl128b-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv32-256b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv32-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zvl256b-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zvl256b-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv32-512b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv32-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zvl512b-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zvl512b-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}

    - name: Download linux rv32-1024b
      uses: ./.github/actions/common/download-comparison-artifacts
      if: ${{ inputs.weekly_run == 'rv32-zvl' }}
      with:
        report-artifact-name: gcc-linux-rv32gcv_zvl1024b-ilp32d-${{ inputs.gcchash }}-non-multilib-report.log
        binary-artifact-name: gcc-linux-rv32gcv_zvl1024b-ilp32d-${{ inputs.gcchash }}-non-multilib
        github-token: ${{ inputs.token }}


